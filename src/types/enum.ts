enum Command {
    CREATE = "create",
    GENERATE = "generate",
    COMPONENT = "component",
    INIT = "init",
}

enum AliasCommand {
    GENERATE = "g",
    COMPONENT = "c",
}

export { Command, AliasCommand };
